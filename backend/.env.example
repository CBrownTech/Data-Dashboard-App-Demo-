# .env.example — commit this file, NOT .env
# Copy this to .env and fill in your real values before running the app.

# Path to your Google Cloud service-account JSON key (BigQuery Data Editor + Job User).
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
# Your GCP project id that owns the BigQuery dataset.
BIGQUERY_PROJECT=your-gcp-project-id
# The BigQuery dataset that holds the application tables.
BIGQUERY_DATASET=bank_db

JWT_SECRET=your-secret-key-change-in-production
